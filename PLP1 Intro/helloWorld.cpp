#include <iostream> /* 
^ this includes some basic functionality, we'll go into more detail later
but it'll be part of pretty much all C++ projrams, as is the std namespace.
*/
using namespace std; //using standard namespace

int main(){  // Normally a function that doesn't return anything would be void, but main must return a value.
    cout << "hello world"; // cout = "console out"
    return 0; // Zero since we don't actually need to return anything
}



